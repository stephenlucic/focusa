name: Deploy Docs (Astro Starlight)

on:
  push:
    branches: [ "main" ]   # ajusta si usas otra rama

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

env:
  DOCS_DIR: Focusa/docs   # <<--- carpeta donde vive tu Astro

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: ${{ env.DOCS_DIR }}/package-lock.json

      - name: Install deps (docs)
        working-directory: ${{ env.DOCS_DIR }}
        run: npm ci

      - name: Build (docs)
        working-directory: ${{ env.DOCS_DIR }}
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ env.DOCS_DIR }}/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
